extends ../layout/_page

block append config
	- var page = pagelist['sitepolicy'];

block append head
	link(rel="stylesheet" href=ROOT_PATH+"css/sitepolicy/style.css")

block content
	// main
	main.main
		h1 #{page.name}
		+breadcrumbs([
			['ホーム', '/'],
			[page.title, page.local_url]
		])
	// /main

block append script
	script(src=ROOT_PATH+"js/sitepolicy.js")